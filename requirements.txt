--extra-index-url https://download.pytorch.org/whl/cu121

edge-tts==7.2.8
faster-whisper==1.2.1
gTTS==2.5.4
numpy>=1.24.4
pygame==2.6.1
sounddevice==0.5.5
torch==2.5.1+cu121
torchaudio==2.5.1+cu121
sentence-transformers>=2.2.2
# transformers>=4.52 redd edir .bin (safetensors olmayan) checkpoint-leri
# torch<2.6 ile (CVE-2025-32434 qorunmasi) - BAAI/bge-m3 yalniz .bin
# formatinda paylanir, torch ise (CUDA/Whisper uygunlugu ucun) 2.5.1-e
# pinlenib. Torch-u yenilemeden problemi aradan qaldirmaq ucun
# transformers bu bloku elave etmezden evvelki versiyaya sabitlenib.
transformers>=4.49,<4.52
requests>=2.31

# RAG: FAISS (dense) + BM25 (sparse) hibrid axtaris - knowledge/rag.py
faiss-cpu>=1.8,<2
rank-bm25>=0.2,<1

# Otel database-i (PostgreSQL + pgvector, Docker) - src/db/
psycopg[binary]>=3.2

# Veb deploy (FastAPI + WebSocket) - src/web/
fastapi>=0.110
uvicorn[standard]>=0.29

# Admin panel (Excel export/import + fayl upload) - src/admin/
openpyxl>=3.1
python-multipart>=0.0.9

# Artiq istifade olunmur (LLM Ollama-ya kecdi, RAG ChromaDB-den FAISS-e):
# llama-cpp-python, chromadb
# Evvelki backend-e qayitmaq lazim gelse geri elave edin.
